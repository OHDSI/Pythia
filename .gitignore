node_modules/
dist/
*.log
.DS_Store
coverage/

# Generated by trex-init on first boot — crypto/JWT keys, never commit.
/secrets/
# Local secrets (Bedrock token, passwords).
.env
# Locally rebuilt WebAPI native lib (large arch-specific binary; built from ../trex).
/native-libs/
/frontend-assets/
# Local agent / browser-automation artifacts.
/.claude/
/.playwright-mcp/
/atlas-*.png
docs/superpowers/

.worktrees/
.gstack/

# Demo recordings. The mp4/webm are large (25-35 MB per take) and would live in
# git history forever — upload the mp4 as a GitHub attachment (issue/release)
# and let the README embed that URL. The small highlight GIF IS committed.
figures/*.mp4
figures/*.webm
!figures/demo-highlight.gif
